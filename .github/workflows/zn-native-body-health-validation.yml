name: ZN Native Body Health Validation

on:
  push:
    branches:
      - work/native-body-health
  workflow_dispatch:

permissions:
  contents: read

env:
  CI: 'true'
  TZ: UTC
  PYTHONUTF8: '1'
  PYTHONUNBUFFERED: '1'
  ZN_AGENT_HOME: ${{ github.workspace }}/.ci/zn-home-native-body-health

defaults:
  run:
    shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command ". '{0}'"

jobs:
  validate:
    name: Native Body health lifecycle
    runs-on: [self-hosted, Windows, X64, zn-ci]
    timeout-minutes: 10
    env:
      UV_PYTHON_INSTALL_DIR: ${{ github.workspace }}/.ci/uv-python
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          version: '0.11.6'
          enable-cache: false
      - name: Prepare isolated runtime
        run: |
          New-Item -ItemType Directory -Force -Path $env:ZN_AGENT_HOME | Out-Null
          uv python install 3.12
          Remove-Item -Recurse -Force .ci/native-body-health-venv -ErrorAction SilentlyContinue
          uv venv .ci/native-body-health-venv --python 3.12
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\native-body-health-venv\Scripts\python.exe'
          uv pip install --python $python runtime/python
      - name: Compile changed resident module
        run: |
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\native-body-health-venv\Scripts\python.exe'
          & $python -m py_compile runtime/python/zn_agent/core/health_aware_resident.py
      - name: Run Native Body health regression tests
        run: |
          $env:PYTHONPATH = Join-Path $env:GITHUB_WORKSPACE 'runtime\python'
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\native-body-health-venv\Scripts\python.exe'
          & $python -m unittest tests.zn_agent.core.test_native_body_health -v
      - name: Run adjacent Body and health regression tests
        run: |
          $env:PYTHONPATH = Join-Path $env:GITHUB_WORKSPACE 'runtime\python'
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\native-body-health-venv\Scripts\python.exe'
          & $python -m unittest tests.zn_agent.core.test_native_body tests.zn_agent.core.test_pointer_body tests.zn_agent.core.test_keyboard_text_body tests.zn_agent.core.test_browser_work_navigation tests.zn_agent.core.test_foreground_window_health tests.zn_agent.core.test_visual_capture_health tests.zn_agent.core.test_cognitive_resource_health -v
