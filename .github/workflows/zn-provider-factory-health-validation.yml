name: ZN Provider Factory Health Validation

on:
  push:
    branches:
      - work/provider-factory-health
  workflow_dispatch:

permissions:
  contents: read

env:
  CI: 'true'
  TZ: UTC
  PYTHONUTF8: '1'
  PYTHONUNBUFFERED: '1'
  ZN_AGENT_HOME: ${{ github.workspace }}/.ci/zn-home-provider-factory-health

defaults:
  run:
    shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command ". '{0}'"

jobs:
  validate:
    name: Provider factory health boundary
    runs-on: [self-hosted, Windows, X64, zn-ci]
    timeout-minutes: 10
    env:
      UV_PYTHON_INSTALL_DIR: ${{ github.workspace }}/.ci/uv-python
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up uv
        uses: astral-sh/setup-uv@v6
        with:
          version: '0.11.6'
          enable-cache: false
      - name: Prepare isolated runtime
        run: |
          New-Item -ItemType Directory -Force -Path $env:ZN_AGENT_HOME | Out-Null
          uv python install 3.12
          Remove-Item -Recurse -Force .ci/provider-factory-health-venv -ErrorAction SilentlyContinue
          uv venv .ci/provider-factory-health-venv --python 3.12
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\provider-factory-health-venv\Scripts\python.exe'
          uv pip install --python $python runtime/python
      - name: Compile provider factory
        run: |
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\provider-factory-health-venv\Scripts\python.exe'
          & $python -m py_compile runtime/python/zn_agent/core/cognitive_factory.py
      - name: Run provider health regression tests
        run: |
          $env:PYTHONPATH = Join-Path $env:GITHUB_WORKSPACE 'runtime\python'
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\provider-factory-health-venv\Scripts\python.exe'
          & $python -m unittest tests.zn_agent.core.test_cognitive_resource_health -v
      - name: Run adjacent provider regression tests
        run: |
          $env:PYTHONPATH = Join-Path $env:GITHUB_WORKSPACE 'runtime\python'
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\provider-factory-health-venv\Scripts\python.exe'
          & $python -m unittest tests.zn_agent.core.test_cognitive_resource tests.zn_agent.core.test_anthropic_resource tests.zn_agent.core.test_gemini_resource -v
