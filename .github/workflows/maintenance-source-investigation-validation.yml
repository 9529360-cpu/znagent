name: Maintenance Source Investigation Validation

on:
  push:
    branches:
      - work/maintenance-source-investigation

permissions:
  contents: read

env:
  CI: 'true'
  PYTHONUTF8: '1'
  PYTHONUNBUFFERED: '1'
  ZN_AGENT_HOME: ${{ github.workspace }}/.ci/zn-home

defaults:
  run:
    shell: powershell -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command ". '{0}'"

jobs:
  validate:
    runs-on: [self-hosted, Windows, X64, zn-ci]
    timeout-minutes: 15
    env:
      UV_PYTHON_INSTALL_DIR: ${{ github.workspace }}/.ci/uv-python
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v6
        with:
          version: '0.11.6'
          enable-cache: false
      - name: Prepare Python
        run: |
          uv python install 3.12
          Remove-Item -Recurse -Force .ci/maintenance-source-venv -ErrorAction SilentlyContinue
          uv venv .ci/maintenance-source-venv --python 3.12
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\maintenance-source-venv\Scripts\python.exe'
          uv pip install --python $python runtime/python
      - name: Compile affected resident modules
        run: |
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\maintenance-source-venv\Scripts\python.exe'
          & $python -m py_compile runtime/python/zn_agent/core/maintenance_source.py runtime/python/zn_agent/core/health_aware_resident.py
      - name: Run maintenance source regressions
        run: |
          $python = Join-Path $env:GITHUB_WORKSPACE '.ci\maintenance-source-venv\Scripts\python.exe'
          $env:PYTHONPATH = Join-Path $env:GITHUB_WORKSPACE 'runtime\python'
          & $python -m unittest -v tests.zn_agent.core.test_maintenance_source_investigation tests.zn_agent.core.test_resident_maintenance_source tests.zn_agent.core.test_maintenance_investigation_lifecycle tests.zn_agent.core.test_resident_maintenance_status
