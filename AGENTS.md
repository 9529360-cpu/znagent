# ZN 仓库维护与接手规则

本文件给任何接手本仓库的维护者使用，包括人类、GPT、Claude、Gemini、Codex 或未来其他模型。

> 重要：这个仓库当前的产品是 **ZN**。Hermes 源码只是成熟机制的参考来源，不是 ZN 的产品身份、主体或最终控制平面。

## 核心原则

**维护者可以随时更换，ZN 项目不能因此中断。**

聊天可以消失，电脑可以更换，模型可以更换；只要仓库和项目级基础设施仍可访问，开发、测试、合并和发布就应能够继续。

事实优先级：

```text
真实代码和 Git 状态
> 实际测试、构建和 CI 结果
> .agent/HANDOFF.md
> 对话描述
```

## 新维护者接手顺序

1. 读取 `ZN.md`。
2. 读取 `docs/ZN-IMPLEMENTATION-STATUS.md`。
3. 读取 `docs/ZN-SOURCE-EXTRACTION.md`。
4. 读取 `docs/ZN-SELF-MAINTENANCE.md`。
5. 读取 `.agent/HANDOFF.md`（存在时）。
6. 检查当前分支、HEAD、相关 diff、PR、CI 和真实代码。
7. 文档与代码冲突时，以真实仓库状态为准，先对账再继续。

新的维护者不需要知道上一位是谁，也不需要拥有上一段聊天记录。

## 分支边界

```text
main          = Hermes 原始/参考基线，M10 前不要修改
dev/zn-agent  = ZN 主开发分支
work/*        = 隔离实验、研究或维护分支
```

普通开发、自维护和实验不得直接在 `main` 试错。

## 开发顺序

```text
检查真实仓库
→ 读 ZN 架构契约
→ 确认当前实现
→ 必要时先更新架构 MD
→ 做最小且完整的修改
→ 补/改测试
→ 跑真实验证
→ 检查 diff
→ 更新状态/HANDOFF
→ 提交/PR/CI
```

可以学习 Hermes 的成熟机制，但最终接口、状态、配置、生命周期和产品身份必须归 ZN。

## 发布能力属于仓库，不属于某个模型

GPT、Claude、Gemini、人类开发者等都只是可替换维护者。

在授权范围内，维护者可以修改代码、建分支、commit、push、创建 PR、查看和修复 CI、按仓库规则合并，以及触发仓库定义的发布流程。

正式发布方法必须保存在仓库自动化中，不能依赖某个模型记得手工步骤。

当前正式发布入口以 `.github/workflows/zn-release.yml` 为准：

```text
可追踪 commit/tag
→ GitHub 自动构建
→ 多平台正式产物
→ 验证正式产物
→ 发布不可变版本文件
→ GitHub Release（需要时）
→ stable.json 最后更新
→ 客户端发现新版
```

客户端不得把 `git pull` 私有仓库最新源码作为正式更新机制。

## 密钥和电脑丢失原则

源码、架构、开发状态、测试、构建和发布规则应尽量存在远程仓库或项目级在线基础设施中。

真正的 Token、对象存储密钥、发布/签名私钥、密码和生产凭证不得提交到仓库。它们应保存在 GitHub Secrets、发布平台安全存储、操作系统安全凭证库或其他项目级安全设施中。

维护者只需要触发流程，不应该需要知道秘密值本身。

目标是：即使开发电脑损坏或丢失，只要仓库账号、必要的项目级在线凭证和账号恢复能力仍在，就可以在新电脑上继续维护和发布。

## 自维护规则

完整规则见 `docs/ZN-SELF-MAINTENANCE.md`。

必须遵守：

- 大模型输出不是事实，必须用代码、日志、测试和真实运行结果验证。
- 不直接修改正在运行的正式安装目录。
- 修复在隔离分支/工作区完成。
- 测试失败不能伪装成成功。
- 不能为了通过而删除有效测试、关闭 CI、关闭更新完整性检查或绕过分支保护。
- 身份、记忆、凭证、更新器、回退、签名、自维护权限等高风险区域默认需要人工批准。
- 新版本失败优先回退旧版，不能牺牲 ZN 身份和记忆连续性。

## HANDOFF：让下一位直接接着干

中大型任务维护 `.agent/HANDOFF.md`。它是施工现场，不是聊天记录。

至少记录当前目标、当前分支和 HEAD、工作状态、已完成事项、任务队列、实际测试/构建/CI 结果、相关文件、风险/阻塞和下一步。

每完成一个有意义阶段后同步更新。不得写入 Token、密码、密钥等敏感信息。

## 测试纪律

不能只凭“代码看起来对”就宣称完成。按改动范围尽量覆盖相关单元测试、集成测试、静态检查、构建/打包，以及必要的端到端、干净安装和版本切换验证。

## 危险操作

以下操作默认需要明确人工确认：强制推送、重写 Git 历史、大量删除、破坏性身份/记忆迁移、删除唯一可回退版本、扩大凭证权限、关闭分支保护/CI/签名/更新完整性检查、把秘密写入仓库，以及让自维护系统修改安全审批规则后自己批准自己。

## 接手成功标准

```text
换 GPT       → 能继续
换 Claude    → 能继续
换其他模型   → 能继续
换人维护     → 能继续
聊天记录丢失 → 能继续
开发电脑丢失 → 换电脑登录仓库后能继续

仓库 + 状态 + CI + 项目级发布基础设施
= 长期开发和发布的连续来源
```

模型是可替换的维护资源；ZN 和它的工程连续性不属于任何一个模型。
