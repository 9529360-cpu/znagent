# ZN 仓库维护与接手规则

本文件给任何接手本仓库的维护者使用，包括人类、GPT、Claude、Gemini、Codex 或未来其他维护系统。

> 这个仓库当前唯一产品是 **ZN**。历史或外部参考源码不是 ZN 的产品身份、主体、运行时或控制平面。

## 核心原则

**维护者可以随时更换，ZN 项目不能因此中断。**

聊天可以消失，电脑可以更换，模型可以更换；只要仓库和项目级基础设施仍可访问，开发、测试、合并和发布就应能够继续。

维护者的首要职责是**推进 ZN 产品本身**。分支、HANDOFF、CI、证据、PR 和 promotion 都是保证工程连续性与安全性的工具，不是工作目标本身。不要为了维护流程而维护流程，也不要因为上一位维护者留下了 Task Queue，就停止重新判断当前最有价值的工程工作。

事实优先级：

```text
真实代码和 Git 状态
> 实际测试、构建和 CI 结果
> .agent/HANDOFF.md
> 对话描述
```

默认自行获取信息、自行做低风险且可逆的工程决策，并推进到可验证结果。只有涉及不可逆操作、明确人工审批边界、权限/凭证、产品方向选择，或无法从仓库可靠推断的高风险歧义时，才停下来询问用户。

## 新维护者接手顺序

1. 读取 `ZN.md`。
2. 读取 `AGENTS.md`。
3. 读取 `docs/ZN-IMPLEMENTATION-STATUS.md`。
4. 读取 `docs/ZN-SOURCE-EXTRACTION.md`。
5. 读取 `docs/ZN-SELF-MAINTENANCE.md`。
6. 读取 `.agent/HANDOFF.md`。
7. 检查 `dev/zn-agent` 与 `main` 当前 HEAD、相关 diff、PR、CI、最近提交。
8. 检查准备修改功能的真实调用链、测试和 active caller。
9. 用当前代码、产品总目标和真实缺口重新判断下一步；HANDOFF 的 Task Queue 是线索和现场记录，不是不可推翻的命令队列。

文档与代码冲突时，以真实仓库状态为准，先对账再继续。新的维护者不需要知道上一位是谁，也不需要拥有上一段聊天记录。

接手完成后不要默认沿着上一阶段的“下一个 evidence slice”机械前进。先回答三个问题：ZN 当前离可用产品/当前里程碑最缺什么？哪个问题风险或价值最高？哪个下一步能形成可验证的完整增量？然后选择最值得做的工作。若答案与 HANDOFF 原计划不同，更新 HANDOFF 并说明原因即可。

## 按任务规模工作

流程应服务于任务，而不是让所有任务都套同一套重流程：

- 小型、局部、低风险修改：定位真实调用链 → 修改 → 相关测试 → diff 检查 → 必要的状态记录。不要为了小修复制造额外项目管理工作。
- 中型功能或跨模块修改：补充架构/状态核对、相关集成测试、CI 和 HANDOFF。
- 大型、跨阶段、发布或高风险工作：使用完整 Task Queue、阶段性 HANDOFF、完整 CI/E2E、promotion gate 和明确风险边界。

测试与安全要求不能因为任务较小而被跳过；这里减少的是不必要的流程负担，不是验证质量。

## 分支边界

M10 canonical promotion 已完成。当前分支职责：

```text
main          = canonical source / release branch
dev/zn-agent  = ZN 固定主开发分支
work/*        = 隔离实验、研究或维护分支
```

普通开发、自维护、调查和实验不得直接在 `main` 试错。正常修改在 `dev/zn-agent` 或隔离 work branch 完成。

这条规则不代表 `main` 应永久冻结。一个低风险、coherent engineering stage 在满足仓库 promotion gate 后，应按正常可追踪 PR / merge / promotion flow 进入 `main`：

```text
完成所声明的实现范围
→ 相关测试通过
→ full CI / 必要 E2E 通过
→ 检查 diff
→ 状态文档与 HANDOFF 对账
→ 无未解决 promotion blocker
→ 不触及需要人工批准的高风险边界
→ 正常 PR / merge / promotion
→ main 成为新的 verified canonical source
```

满足这些条件的正常低风险 promotion 不需要额外依赖某一段聊天里再说一句“可以合并”。但是身份、长期记忆、破坏性数据迁移、凭证/权限、updater/rollback/signing、自维护审批规则，以及替换用户当前正式安装版本等高风险边界，仍必须保留明确人工批准。

禁止为了“同步方便”对 `main` 或 `dev/zn-agent` 使用 force push、历史重写、绕过失败 CI 或伪造完成状态。

## 开发顺序

下面是中大型任务的默认闭环，不要求机械执行无关步骤：

```text
检查真实仓库
→ 读相关 ZN 架构契约
→ 确认当前实现与 active caller
→ 自主选择最小且完整的方案
→ 修改代码
→ 补/改相关测试
→ 跑真实验证
→ 修复失败
→ 检查 diff / 安全 / 回归风险
→ commit / push
→ 检查必要 CI
→ 更新必要状态/HANDOFF
→ 达到 promotion gate 时按正常流程推进 canonical source
```

能从仓库、代码、测试、CI、日志查清楚的，不要反复询问用户。发现与当前目标直接相关、低风险且明显的 bug、测试缺口、错误处理问题或维护性问题，可以顺手修复；不要借机做无关的大规模重构。

完成一个阶段后，必须重新评估产品缺口和优先级，而不是把“生成下一份证据”本身当作默认目标。证据应证明真实产品能力，不应取代产品能力建设。

## ZN-only ownership 边界

活跃开发树必须保持 ZN-only。历史或外部参考实现可以用于理解成熟机制、异常边界和测试思路，但不得重新成为：

- ZN runtime；
- resident 主循环；
- ZN UI / Electron main / preload；
- gateway brain；
- Python distribution；
- 正式构建或发布依赖；
- 产品控制面。

需要参考成熟实现时：

```text
ZN 有具体需求
→ 阅读 dedicated reference branch、Git 历史或外部参考实现
→ 理解机制和边界
→ 提取/适配最小完整机制
→ 放入 ZN-owned namespace/interface/config/state/lifecycle
→ 删除参考产品假设
→ 增加 ZN 行为测试
→ 切换 active caller
```

不要为了通过测试恢复历史产品路径、兼容层或旧控制面。

## 发布能力属于仓库，不属于某个模型

GPT、Claude、Gemini、人类开发者等都只是可替换维护者。

在授权范围内，维护者可以修改代码、建分支、commit、push、创建 PR、查看和修复 CI、按仓库规则合并，以及触发仓库定义的发布流程。

正式发布方法必须保存在仓库自动化中，不能依赖某个模型记得手工步骤。

当前正式发布入口以 `.github/workflows/zn-release.yml` 为准：

```text
可追踪 commit/tag
→ GitHub 自动构建
→ 正式产物
→ 验证正式产物
→ 发布不可变版本文件
→ GitHub Release（需要时）
→ stable.json 最后更新
→ 客户端发现新版
```

客户端不得把 `git pull` 私人仓库最新源码作为正式更新机制。

## 密钥和电脑丢失原则

源码、架构、开发状态、测试、构建和发布规则应尽量存在远程仓库或项目级在线基础设施中。

真正的 Token、对象存储密钥、发布/签名私钥、密码和生产凭证不得提交到仓库。它们应保存在 GitHub Secrets、发布平台安全存储、操作系统安全凭证库或其他项目级安全设施中。

维护者只需要触发流程，不应该需要知道秘密值本身。

## 自维护规则

完整规则见 `docs/ZN-SELF-MAINTENANCE.md`。

必须遵守：

- 大模型输出不是事实，必须用代码、日志、测试和真实运行结果验证；
- 不直接修改正在运行的正式安装目录；
- 修复在开发分支/隔离分支/工作区完成；
- 测试失败不能伪装成成功；
- 不能为了通过而删除有效测试、关闭 CI、关闭更新完整性检查或绕过分支保护；
- 身份、记忆、凭证、更新器、回退、签名、自维护权限等高风险区域默认需要人工批准；
- 新版本失败优先回退旧版，不能牺牲 ZN 身份和记忆连续性。

## HANDOFF

中大型任务维护 `.agent/HANDOFF.md`。它是施工现场和事实索引，不是聊天记录，也不是下一位维护者必须逐条照做的剧本。

至少记录：

- 当前目标；
- 当前分支和 HEAD；
- 已完成事项；
- Task Queue；
- 实际测试/构建/CI 结果；
- 相关文件；
- 风险/阻塞；
- 下一步建议及其依据。

Task Queue 应保持短小、可验证、按真实优先级排序。阶段结束或仓库状态变化后，下一位维护者可以基于真实产品缺口重新排序、合并、删除或替换任务；只需让 HANDOFF 与实际状态重新一致。不要让过期队列把维护者锁进旧计划。

每完成一个有意义阶段后同步更新。不得写入 Token、密码、密钥等敏感信息。

## 测试纪律

不能只凭“代码看起来对”就宣称完成。按改动范围覆盖相关单元测试、集成测试、静态检查、构建/打包，以及必要的端到端、干净安装和版本切换验证。

验证强度与风险匹配。文档或维护规则的小改动不应为了“流程完整”强制重复昂贵且与改动无关的产品 E2E；产品代码、构建、安装、更新、身份/记忆等改动则必须运行相应的真实验证。代码修改完成后，优先让仓库自己的 CI 成为可恢复的权威验证来源。

## 危险操作

以下操作默认需要明确人工确认：

- 强制推送；
- 重写 Git 历史；
- 大规模删除；
- 破坏性身份/长期记忆/生产数据迁移；
- 删除唯一可回退版本；
- 扩大凭证权限；
- 关闭分支保护/CI/签名/更新完整性检查；
- 把秘密写入仓库；
- 让自维护系统修改安全审批规则后自己批准自己。

其他正常、可逆、与明确目标一致的开发工作，以及满足 promotion gate 的低风险正常 promotion，不要反复依赖聊天确认。

## 接手成功标准

```text
换 GPT       → 能继续，而且会重新判断当前最值得做的事
换 Claude    → 能继续，而且会重新判断当前最值得做的事
换其他模型   → 能继续，而且会重新判断当前最值得做的事
换人维护     → 能继续
聊天记录丢失 → 能继续
开发电脑丢失 → 换电脑登录仓库后能继续

仓库 + 状态 + CI + 项目级发布基础设施
= 长期开发和发布的连续来源
```

模型是可替换的维护资源；ZN 和它的工程连续性不属于任何一个模型。